Run the following command in your local machine:

```bash copy
git clone https://github.com/cloudcode/cloudcode.git
```

This command will clone the CloudCode repository from GitHub to your local machine.